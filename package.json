{
  "name": "hotel-website",
  "version": "1.0.0",
  "description": "Hotel booking website",
  "main": "Backend/server.js",
  "scripts": {
    "start": "cd Backend && npm install && npm run seed && npm start",
    "build": "cd Backend && npm install",
    "dev": "cd Backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}